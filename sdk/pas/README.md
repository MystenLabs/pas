# PAS SDK

> The SDK is hard to setup until the 29th of Jan, as SDK v2 has not yet been released to npm. For
> this to work, you need to have the `ts-sdks` repo in a path 1 level back of this project. This
> will be fixed when sdk v2 gets released on npm.
